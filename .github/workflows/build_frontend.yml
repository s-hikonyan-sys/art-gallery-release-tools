name: Build Frontend

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: 'ビルド対象のリリースバージョン (例: v1.0.0。未指定時はリポジトリ変数を参照)'
        required: false
        type: string
      frontend_ref:
        description: 'ビルドする frontend リポジトリの ref (branch/tag/sha)。未指定時は release_version を使用'
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Resolve Context
        id: context
        run: |
          VERSION="${{ github.event.inputs.release_version || vars.RELEASE_VERSION }}"
          if [ -z "$VERSION" ]; then
            echo "::error::RELEASE_VERSION が定義されていません。"
            exit 1
          fi

          REF_INPUT="${{ github.event.inputs.frontend_ref }}"
          if [ -z "$REF_INPUT" ]; then
            REF_INPUT="$VERSION"
          fi

          echo "RELEASE_VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "FRONTEND_REF=${REF_INPUT}" >> "$GITHUB_ENV"

      - name: Checkout frontend repository
        uses: actions/checkout@v4
        with:
          repository: s-hikonyan-sys/art-gallery-frontend
          ref: ${{ env.FRONTEND_REF }}
          token: ${{ secrets.GH_TOKEN_FOR_ART_GALLERY_FRONTEND }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint check
        run: npm run lint:check

      - name: Run tests
        run: npm run test:run

      - name: Build
        run: npm run build

      - name: Upload versioned dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist-${{ env.RELEASE_VERSION }}
          path: dist/
          retention-days: 30
