---
# バックエンドデプロイロール
# 環境別処理と用途別処理を統合

- name: Deploy backend (prepare + source)
  ansible.builtin.include_tasks: deploy_only.yml

- name: Include verify tasks
  ansible.builtin.include_tasks: verify_only.yml
