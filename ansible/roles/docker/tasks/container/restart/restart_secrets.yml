---
# Secrets API コンテナの再起動

- name: Restart secrets-api container
  ansible.builtin.shell: |
    cd {{ deployment_paths.deployment_dir }}
    docker compose restart secrets-api
  tags:
    - docker
    - restart-secrets