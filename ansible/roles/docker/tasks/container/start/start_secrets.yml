---
# Secrets API コンテナの起動
# イメージが存在しない場合はプルを試みます

- name: Start secrets-api container
  ansible.builtin.shell: |
    cd {{ deployment_paths.deployment_dir }}
    docker compose up -d secrets-api
  become: no
  tags:
    - docker
    - start-secrets
