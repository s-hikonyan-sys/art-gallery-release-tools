# 本番環境用Vault設定ファイル（Ansible Vaultで暗号化）
# このファイルは ansible-vault create で直接作成してください
# 
# 作成方法:
#   cd ansible
#   ansible-vault create vault/vault-config.yaml.vault.prod --vault-password-file .vault_pass
#
# 編集方法:
#   cd ansible
#   ansible-vault edit vault/vault-config.yaml.vault.prod --vault-password-file .vault_pass
#
# 注意: このファイルは実際には暗号化されているため、この内容は表示されません
# 実際のファイルは ansible-vault create で生成してください
#
# 記述する内容（復号化後の例）:
# database:
#   password: prod-database-password-here
# secret_key: CHANGE-THIS-TO-STRONG-RANDOM-KEY-IN-PRODUCTION-MINIMUM-32-CHARACTERS
#
# 注意: 
# - このファイルにはdatabase.passwordとsecret_keyを含めます（master_passwordは不要）
# - secret_keyはFernet暗号化に使用されるキーです（最低32文字以上推奨）
# - 本番環境では強力なランダム文字列を設定してください
# - ファイル名はsecrets.yaml.vaultとして使用されます
# - vault-config.yaml（プレーンテキスト）は作成しません
# - コマンドライン引数や環境変数でパスワードを渡しません
# - 本番環境用の強力なマスターパスワードを使用してください
# - 開発環境とは異なるパスワードを必ず使用してください
