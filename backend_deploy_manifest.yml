---
# Backend production deploy history (Deploy Backend workflow appends on success).
# deploy_type: code | image | both
# release_version: イメージタグ（GHCR / backend_version_manifest.yml のキー）
# code_version: コードマニフェストのキー（任意。未指定時は backend_ref 運用）
# ref_commit_sha: コード側のコミット（backend_code_manifest または git ls-remote）
# image_source_sha: イメージビルド時のソースコミット（backend_version_manifest.yml）

backend_deployments: []
